# Frontend (UI do projeto original)

## Rodar local
```bash
npm i
cp .env.example .env
npm run dev
```

Abra: http://localhost:5173

## Configurar API
Defina `VITE_API_URL` no `.env` apontando para o backend (Railway ou local).

## Rota do painel financeiro
- `/admin/finance`
