# Frontend will call the backend API using this base URL.
# In Railway: set to your backend public domain, e.g. https://xxxx.up.railway.app
VITE_API_URL=http://localhost:3000
# Tenant used for demo requests (can override in UI)
VITE_DEFAULT_TENANT_ID=
