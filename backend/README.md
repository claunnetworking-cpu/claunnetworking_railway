# Backend (Railway Ready)

## Endpoints
- `GET /health`
- `GET /dashboard/metrics` (requer `x-tenant-id`)
- `GET /metrics` (Prometheus)

## Local
```bash
cp ../.env.example .env
npm i
npm run dev
```
